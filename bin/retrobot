#!/usr/bin/env ruby
retried = false
begin
  require 'retrobot'
rescue LoadError
  raise if retried
  retried = true
  $LOAD_PATH.unshift "#{__dir__}/../lib"
  retry
end

$stdout.sync = true
Retrobot.new(ARGV).main
